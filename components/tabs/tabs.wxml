<view class="tabs">
  <view class="tab-title">
    <!-- <view class="tab-item active">分享</view>
    <view class="tab-item">记录</view>
    <view class="tab-item">私信</view> -->
    <view wx:for="{{tabs}}" wx:key="id" class="tab-item {{item.isActive?'active':''}}" bindtap="handleItemTap" data-index="{{index}}">
      {{item.name}}
    </view>
  </view>
</view>


