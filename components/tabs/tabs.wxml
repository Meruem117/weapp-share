<view class="tabs">
  <view class="tab-title">
    <view wx:for="{{tabs}}" wx:key="id" class="tab-item {{item.isActive?'active':''}}" bindtap="handleItemTap" data-index="{{index}}">
      {{item.name}}
    </view>
  </view>
</view>